// there a couple "simple" or "primitive" types

const numbersDawg = 10000; // any number or decimal number

const stringDawg = 'wow'; // any text surrounded by quotes

const booleanDawg = true; // true / false

const undefinedDawg = undefined; // value of nothing

const nullDawg = null; // value of nothing but with gusto

// sometimes you'll need to change between these types...
// JavaScript has a long list of global functions, but you worry about that later

// the Number function takes in a value and converts it to a primitive number type
const converted1000Number = Number('10000');

// the Boolean function takes in a value and converts it to a primitive boolean type 
const convertedFalseBoolean = Boolean(0);
const convertedTrueBoolean = Boolean(1);


// the String function takes in a value and converts it to a primitive string type
const converted1000String = String(1000);

